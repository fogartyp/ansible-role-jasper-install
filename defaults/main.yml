---
# defaults file for ansible-role-jasper-install
staging_dir: "{{ lookup('env','staging_dir') }}"
appServerType: "{{ lookup('env','appServerType') }}"
appServerDir: "{{ lookup('env','appServerDir') }}"
dbType: "{{ lookup('env','dbType') }}"
dbUsername: "{{ lookup('env','dbUsername') }}"
dbPassword: "{{ lookup('env','dbPassword') }}"
sysUsername: "{{ lookup('env','sysUsername') }}"
sysPassword: "{{ lookup('env','sysPassword') }}"
dbHost: "{{ lookup('env','dbHost') }}"
dbPort: "{{ lookup('env','dbPort') }}"
sid: "{{ lookup('env','sid') }}"
jdbcDataSourceClass: "{{ lookup('env','jdbcDataSourceClass') }}"
maven_jdbc_groupId: "{{ lookup('env','maven_jdbc_groupId') }}"
maven_jdbc_artifactId: "{{ lookup('env','maven_jdbc_artifactId') }}"
maven_jdbc_version: "{{ lookup('env','maven_jdbc_version') }}"
jdbcDriverClass: "{{ lookup('env','jdbcDriverClass') }}"
db_server_exists: "{{ lookup('env','db_server_exists') }}"
app_server_exists: "{{ lookup('env','app_server_exists') }}"
